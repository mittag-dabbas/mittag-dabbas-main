'use strict';

/**
 * available-delivery router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::available-delivery.available-delivery');
