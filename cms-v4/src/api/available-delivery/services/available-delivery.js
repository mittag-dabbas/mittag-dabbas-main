'use strict';

/**
 * available-delivery service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::available-delivery.available-delivery');
